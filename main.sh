#Calling Blender in headless mode to do a geometry “crunch.”
#NOTE: different unix flavors will have different paths for blender
#      and the target script will likely be another location.
#      -b = headless mode
#      -P = path to input file
/Applications/blender.app/Contents/MacOS/blender -b -P export_stl.py